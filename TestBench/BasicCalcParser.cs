namespace TestBench
{
	/// <summary>
	/// Value ← [0-9]+ / '(' Expr ')'
	/// Product ← Value (('*' / '/') Value)*
	/// Sum ← Product (('+' / '-') Product)*
	/// Expr ← Sum
	/// </summary>
	public class BasicCalcParser
	{
		
	}
}